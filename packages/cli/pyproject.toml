[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vexa-cli"
version = "1.0.55"
description = "VEXA Command Line Interface — air-gapped security scanning with AI fixes"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{name = "Noviq Technologies", email = "support@noviqtechnologies.com"}]
keywords = ["security", "cli", "scanner", "vulnerability", "sast"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
]


dependencies = [
    "vexa-core>=1.0.35",
    "click>=8.0.0",
    "rich>=13.0.0",
]

[project.urls]
Homepage = "https://usevexa.dev"
Repository = "https://github.com/noviqtechnologies/vexa-oss"

[project.scripts]
vexa = "vexa_cli.main:main_entry"

[tool.setuptools.packages.find]
where = ["src"]
include = ["vexa_cli*"]

[tool.uv.sources]
vexa-core = { workspace = true }
